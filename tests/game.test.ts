import { describe, it, expect } from 'vitest';
import { comboBonus, expToNextLevel, progressiveTarget, regressTarget, staticDiminishedExp, vaultDeposit, vaultBoost, isoDay, streakFromDates, longestStreakFromDates } from '@/lib/game';
import { onboardingCategories, wantsStretchHabit } from '@/lib/onboarding';
describe('game engine',()=>{it('maps onboarding answers to an intentional starter routine',()=>{expect(onboardingCategories('Energy and movement')).toEqual(['HEALTH','NUTRITION']);expect(onboardingCategories('Focus and calm')).toEqual(['MENTAL']);expect(wantsStretchHabit('A little challenging')).toBe(true)});it('uses the EXP curve',()=>expect(expToNextLevel(1)).toBe(100));it('scales targets and honors overrides',()=>{expect(progressiveTarget(10,5,1.5)).toBe(17);expect(progressiveTarget(10,5,1.5,30)).toBe(30)});it('regresses gently but never below base',()=>{expect(regressTarget(30,10)).toBe(24);expect(regressTarget(11,10)).toBe(10)});it('calculates combo and diminishing returns',()=>{expect(comboBonus(101,true)).toBe(20);expect(staticDiminishedExp(100,6)).toBe(50)});it('banks and applies vault EXP',()=>{expect(vaultDeposit(200,100,100)).toBe(100);expect(vaultBoost(100,true)).toBe(25)});it('buckets today in the account timezone',()=>expect(isoDay(new Date('2025-01-01T01:00:00Z'),'America/New_York')).toBe('2024-12-31'));it('calculates current and longest streaks from distinct calendar days',()=>{const days=['2026-08-13','2026-08-14','2026-08-14','2026-08-16','2026-08-17','2026-08-18'];expect(streakFromDates(days,'2026-08-18',false)).toBe(3);expect(longestStreakFromDates(days)).toBe(3)});it('returns no current streak when today is not completed, while preserving historical best',()=>{const days=['2026-08-01','2026-08-02','2026-08-03'];expect(streakFromDates(days,'2026-08-05',false)).toBe(0);expect(longestStreakFromDates(days)).toBe(3)})});
