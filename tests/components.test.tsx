import React from 'react';import {describe,it,expect,vi} from 'vitest';import {render,screen,fireEvent,act} from '@testing-library/react';import {TaskCard} from '@/components/TaskCard';import {ExpBar} from '@/components/ExpBar';import {Heatmap} from '@/components/Heatmap';
describe('components',()=>{it('completes a task',async()=>{const fn=vi.fn();render(<TaskCard task={{id:'a',icon:'⚡',name:'Walk',category:'HEALTH',exp:20}} onComplete={fn}/>);await act(async()=>{fireEvent.click(screen.getByLabelText('Complete Walk'))});expect(fn).toHaveBeenCalledWith('a')});it('renders EXP and heatmap',()=>{render(<><ExpBar current={50} next={100}/><Heatmap values={[80]}/></>);expect(screen.getByLabelText('Experience 50 of 100')).toBeInTheDocument();expect(document.querySelectorAll('.heat')).toHaveLength(365)})});
