'use client'
import React from 'react'
import { AnimatePresence, motion, useReducedMotion } from 'framer-motion'
import { Sparkles, Trophy } from 'lucide-react'
import { Button } from '@/components/ui/button'
export function LevelUpModal({level,palette,onClose}:{level:number|null;palette:string|null;onClose:()=>void}){const reduced=useReducedMotion();return <AnimatePresence>{level!==null&&<motion.div className="fixed inset-0 z-50 grid place-items-center bg-[rgb(var(--ink)/.72)] p-5" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.section role="dialog" aria-modal="true" aria-label="Level up" className="relative w-full max-w-sm overflow-hidden border-4 border-border bg-accent p-6 text-center text-foreground shadow-[8px_8px_0_rgb(var(--shadow))]" initial={reduced?false:{scale:.65,rotate:-5}} animate={{scale:1,rotate:0}} transition={{type:'spring',stiffness:250,damping:16}}><motion.div className="pointer-events-none absolute inset-0 opacity-25" animate={reduced?{}:{rotate:[0,4,-4,0]}}><div className="absolute left-4 top-4 size-5 bg-brand"/><div className="absolute right-8 top-12 size-4 bg-white"/><div className="absolute bottom-7 left-10 size-4 bg-brand"/></motion.div><Sparkles className="relative mx-auto size-10"/><p className="relative mt-4 font-black uppercase tracking-widest">Level up!</p><motion.p className="relative mt-1 text-7xl font-black leading-none" initial={{scale:.4}} animate={{scale:1}} transition={{delay:.15,type:'spring'}}>{level}</motion.p><p className="relative mt-4 text-sm font-bold">Your momentum is getting loud.</p>{palette&&<motion.div className="relative mt-5 border-3 border-border bg-brand p-3 text-white" initial={reduced?false:{x:-12,opacity:0}} animate={{x:0,opacity:1}} transition={{delay:.3}}><Trophy className="mx-auto size-5"/><b className="mt-1 block">{palette} unlocked!</b></motion.div>}<Button className="relative mt-6 w-full" onClick={onClose}>Keep going</Button></motion.section></motion.div>}</AnimatePresence>}
