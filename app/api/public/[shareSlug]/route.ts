import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {levelTitle} from '@/lib/game';
export async function GET(_:Request,{params}:{params:{shareSlug:string}}){const user=await prisma.user.findUnique({where:{shareSlug:params.shareSlug},include:{badges:{include:{badge:true}},logs:{select:{date:true,expEarned:true}}}});if(!user||!user.publicProfile)return NextResponse.json({error:'Not found'},{status:404});return NextResponse.json({name:user.displayName,level:user.level,title:levelTitle(user.level),badges:user.badges.map(b=>b.badge),logs:user.logs.map(log=>({date:log.date,expEarned:log.expEarned}))});}
