import { NextResponse } from 'next/server';
import { z } from 'zod';
import { currentUser } from '@/lib/current-user';
import { prisma } from '@/lib/prisma';
import { onboardingCategories, wantsStretchHabit } from '@/lib/onboarding';
const input=z.object({answers:z.array(z.string()).length(3)});
export async function POST(request:Request){const user=await currentUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});try{const {answers}=input.parse(await request.json());const categories=onboardingCategories(answers[0]);const starters=await prisma.task.findMany({where:{isStarterTask:true}});const selected=starters.filter(task=>categories.includes(task.category)).map(task=>task.id);await prisma.userTask.updateMany({where:{userId:user.id,taskId:{in:starters.map(task=>task.id)}},data:{isPaused:true}});if(selected.length)await prisma.userTask.updateMany({where:{userId:user.id,taskId:{in:selected}},data:{isPaused:false}});if(wantsStretchHabit(answers[2])){const stretch=await prisma.task.findFirst({where:{name:'Stretch'}});if(stretch)await prisma.userTask.upsert({where:{userId_taskId:{userId:user.id,taskId:stretch.id}},update:{isPaused:false},create:{userId:user.id,taskId:stretch.id}})}await prisma.user.update({where:{id:user.id},data:{onboardingCompletedAt:new Date()}});return NextResponse.json({ok:true,categories})}catch{return NextResponse.json({error:'Please complete each onboarding question.'},{status:400})}}
