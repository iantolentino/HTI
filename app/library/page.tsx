'use client';
import { useEffect, useMemo, useState } from 'react';
import { Layers3, Plus, Search, Sparkles } from 'lucide-react';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Card } from '@/components/ui/card';
import { Nav } from '@/components/Nav';

type Task = { id:string; name:string; icon:string; category:string; difficultyTag:string; whyThisHabit:string; type:string; baseTarget:number|null; unit:string|null };
type LibraryData = { tasks:Task[]; owned:{taskId:string;isPaused:boolean}[]; stacks:{id:string;name:string;description:string;tasks:{task:Task}[]}[] };

export default function Library() {
  const [data, setData] = useState<LibraryData | null>(null); const [query, setQuery] = useState(''); const [filter, setFilter] = useState('ALL'); const [notice, setNotice] = useState('');
  const load = async () => { const r = await fetch('/api/habits'); if (r.ok) setData(await r.json()); }; useEffect(() => { void load(); }, []);
  const items = useMemo(() => data?.tasks.filter(task => (filter === 'ALL' || task.difficultyTag === filter) && task.name.toLowerCase().includes(query.toLowerCase())) ?? [], [data, filter, query]);
  async function add(taskId:string) { const r = await fetch('/api/habits', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({taskId}) }); setNotice(r.ok ? 'Added to today’s list.' : 'Could not add this habit.'); if (r.ok) void load(); }
  if (!data) return <main className="shell"><Card className="animate-pulse p-6"><div className="h-8 w-56 rounded bg-[rgb(var(--primary)/.12)]" /><div className="mt-5 h-12 rounded bg-[rgb(var(--primary)/.08)]" /></Card><Nav /></main>;
  return <main className="shell"><header className="mb-6"><p className="eyebrow">BUILD YOUR ROUTINE</p><h1 className="mt-2 text-3xl font-black tracking-tight">Habit library</h1><p className="mt-2 text-[rgb(var(--muted))]">Choose habits that fit the life you actually live.</p></header><div className="relative"><Search className="pointer-events-none absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-[rgb(var(--muted))]" /><input aria-label="Search habits" value={query} onChange={e => setQuery(e.target.value)} placeholder="Search habits" className="h-12 w-full rounded-2xl border border-[rgb(var(--ink)/.10)] bg-[rgb(var(--surface))] pl-12 pr-4 outline-none transition focus:border-[rgb(var(--primary)/.5)] focus:ring-4 focus:ring-[rgb(var(--primary)/.10)]" /></div><div className="mt-3 flex gap-2 overflow-x-auto pb-1">{['ALL','EASY','MEDIUM','HARD'].map(value => <Button key={value} type="button" onClick={() => setFilter(value)} variant={filter === value ? 'primary' : 'secondary'} className="shrink-0">{value === 'ALL' ? 'All levels' : value[0] + value.slice(1).toLowerCase()}</Button>)}</div><Card className="mt-6 p-5"><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-[rgb(var(--accent)/.14)]"><Layers3 className="h-5 w-5 text-[rgb(var(--accent))]" /></div><div><h2 className="font-black">Habit stacks</h2><p className="mt-1 text-sm text-[rgb(var(--muted))]">One tap for a ready-made rhythm.</p></div></div>{data.stacks.map(stack => <div key={stack.id} className="mt-4 flex items-center gap-3 border-t border-[rgb(var(--ink)/.08)] pt-4"><div className="min-w-0 flex-1"><b>{stack.name}</b><p className="mt-1 text-sm text-[rgb(var(--muted))]">{stack.description}</p></div><Button variant="secondary" onClick={() => void Promise.all(stack.tasks.map(item => add(item.task.id)))}><Plus className="h-4 w-4" />Add</Button></div>)}</Card>{notice && <p role="status" className="mt-4 rounded-xl bg-[rgb(var(--primary)/.10)] p-3 text-sm font-semibold text-[rgb(var(--primary))]">{notice}</p>}<div className="mt-6 grid gap-3">{items.map(task => { const present = data.owned.some(owned => owned.taskId === task.id && !owned.isPaused); return <Card className="flex gap-3 p-4" key={task.id}><div className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-[rgb(var(--primary)/.10)] text-2xl">{task.icon}</div><div className="min-w-0 flex-1"><Badge>{task.category.replace('_',' ')} · {task.difficultyTag}</Badge><h2 className="mt-2 font-extrabold">{task.name}</h2><p className="mt-1 text-sm leading-6 text-[rgb(var(--muted))]"><Sparkles className="mr-1 inline h-3.5 w-3.5 text-[rgb(var(--accent))]" />{task.whyThisHabit}</p></div><Button variant="secondary" disabled={present} onClick={() => void add(task.id)} className="self-center px-3">{present ? 'Added' : <><Plus className="h-4 w-4" />Add</>}</Button></Card>; })}</div><Nav /></main>;
}
