'use client';import {useState} from 'react';import {Nav} from '@/components/Nav';
const habits=[['🌅','Make your bed','SELF-CARE','EASY','A small completed promise starts a chain reaction.'],['🏃','Run outside','HEALTH','HARD','A little daylight and movement improves your mood.'],['📝','Journal','MENTAL','MEDIUM','Naming your thoughts makes room for clearer ones.'],['💧','Drink water','NUTRITION','EASY','Hydration supports your energy all day.'],['🧘','Stretch','HEALTH','EASY','Gentle movement keeps your body ready for life.'],['🍲','Cook a nourishing meal','NUTRITION','MEDIUM','Cooking lets you care for future you.']];
export default function Library(){const [added,setAdded]=useState<string[]>([]);const [query,setQuery]=useState('');const filtered=habits.filter(h=>h[1].toLowerCase().includes(query.toLowerCase()));return <main className="shell"><h1 className="text-3xl font-black">Habit Library</h1><p className="mt-1 text-[rgb(var(--muted))]">Build a routine that fits your real life.</p><input aria-label="Search habits" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search habits" className="mt-5 w-full rounded-xl border bg-surface p-3"/><section className="mt-5 card p-4"><p className="font-black">Habit stacks</p><p className="mt-1 text-sm text-[rgb(var(--muted))]">Morning Routine · Make bed, stretch, water</p><button className="btn btn-secondary mt-3">Add this stack</button></section><div className="mt-5 grid gap-3">{filtered.map(h=><article className="card flex gap-3 p-4" key={h[1]}><span className="text-3xl">{h[0]}</span><div className="flex-1"><p className="text-xs font-bold text-brand">{h[2]} · {h[3]} ★</p><h2 className="font-bold">{h[1]}</h2><p className="mt-1 text-sm text-[rgb(var(--muted))]">{h[4]}</p></div><button onClick={()=>setAdded(a=>[...a,h[1]])} className="btn btn-secondary self-center">{added.includes(h[1])?'Added':'Add'}</button></article>)}</div><Nav/></main>}
