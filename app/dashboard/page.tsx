'use client'
import { useEffect, useState } from 'react'
import Link from 'next/link'
import { ArrowRight, Flame, Plus, TrendingUp } from 'lucide-react'
import { ExpBar } from '@/components/ExpBar'
import { TaskCard, Habit } from '@/components/TaskCard'
import { Card } from '@/components/ui/card'
import { Nav } from '@/components/Nav'
import { LevelUpModal } from '@/components/LevelUpModal'

type Dashboard={user:{displayName:string;level:number;title:string;totalExp:number;nextExp:number};tasks:Habit[];done:number;growth:number|null;streak:number;discoverUnlocked:boolean;welcomeBack:{daysMissed:number;message:string}|null;weeklyRecap:{bestDay:string|null;consistentCategory:string;weakestCategory:string}|null;challenge:{id:string;title:string;description:string;exp:number;completedAt:string|null}}
export default function Dashboard(){
 const [data,setData]=useState<Dashboard|null>(null);const [toast,setToast]=useState('');const [levelUp,setLevelUp]=useState<{level:number;palette:string|null}|null>(null);const load=async()=>{try{const r=await fetch('/api/dashboard');setData(r.ok?await r.json():null)}catch{setData(null)}};useEffect(()=>{void load()},[])
 const notify=(message:string)=>{setToast(message);setTimeout(()=>setToast(''),3600)}
 async function complete(id:string){const r=await fetch('/api/complete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userTaskId:id})});const x=await r.json();if(!r.ok){notify(x.message??x.error??'Could not record this win.');return}const extra=[x.combo?`+${x.combo} combo`:null,x.vault?`+${x.vault} vault`:null,x.perfectWeekBonus?`+${x.perfectWeekBonus} perfect week`:null,x.unlockedPalette?`${x.unlockedPalette} unlocked`:null].filter(Boolean).join(' · ');notify(`+${x.gained} EXP${extra?` · ${extra}`:''} — nice work.`);if(x.leveled)setLevelUp({level:x.level,palette:x.unlockedPalette??null});void load()}
 async function completeChallenge(){const r=await fetch('/api/challenge',{method:'POST'});const x=await r.json();notify(r.ok?`Monthly challenge complete · +${x.exp} EXP`:x.error);if(r.ok)void load()}
 if(levelUp)return <LevelUpModal level={levelUp.level} palette={levelUp.palette} onClose={()=>setLevelUp(null)}/>
 if(!data)return <main className="shell"><Card className="animate-pulse p-5"><div className="h-7 w-48 rounded bg-brand/10"/><div className="mt-5 h-3 rounded bg-brand/10"/><div className="mt-5 grid grid-cols-2 gap-3"><div className="h-20 rounded bg-brand/10"/><div className="h-20 rounded bg-brand/10"/></div></Card><Nav/></main>
 return <main className="shell"><Card className="relative overflow-hidden p-4 sm:p-7"><div className="pointer-events-none absolute -right-16 -top-16 size-44 rounded-full bg-brand/10 blur-2xl"/><div className="relative flex items-start justify-between gap-3"><div className="min-w-0"><p className="eyebrow">TODAY’S QUEST</p><h1 className="mt-2 truncate text-2xl font-black tracking-tight">Hi, {data.user.displayName}</h1><p className="mt-1 text-sm text-muted-foreground">Level {data.user.level} · {data.user.title}</p></div><div className="shrink-0 rounded-2xl bg-accent/15 px-3 py-2 text-center"><b className="block text-lg">{data.done}/{data.tasks.length}</b><span className="text-[10px] font-bold uppercase tracking-wider text-muted-foreground">wins</span></div></div><div className="relative mt-6"><ExpBar current={data.user.totalExp} next={data.user.nextExp}/></div><div className="relative mt-5 grid grid-cols-2 gap-3"><div className="rounded-2xl bg-brand/10 p-4"><span className="flex items-center gap-1.5 text-[10px] font-black uppercase tracking-wider text-brand"><Flame className="size-3.5"/>Streak</span><b className="mt-1 block text-xl">{data.streak} day{data.streak===1?'':'s'}</b></div><div className="rounded-2xl bg-accent/15 p-4"><span className="flex items-center gap-1.5 text-[10px] font-black uppercase tracking-wider text-brand"><TrendingUp className="size-3.5"/>Growth</span><b className="mt-1 block text-xl">{data.growth===null?'Starting':`${data.growth>0?'+':''}${data.growth}%`}</b></div></div></Card>{data.welcomeBack&&<Card className="mt-4 border-brand/20 bg-brand/5 p-4"><b>{data.welcomeBack.message}</b><p className="mt-1 text-sm text-muted-foreground">{data.welcomeBack.daysMissed} day{data.welcomeBack.daysMissed===1?'':'s'} passed. Your next small win still counts.</p></Card>}{data.weeklyRecap&&<Card className="mt-4 p-4"><p className="eyebrow">WEEKLY RECAP</p><p className="mt-2 text-sm">Best day: <b>{data.weeklyRecap.bestDay??'Still to come'}</b> · Strongest: <b>{data.weeklyRecap.consistentCategory.replace('_',' ')}</b> · A gentle nudge: <b>{data.weeklyRecap.weakestCategory.replace('_',' ')}</b></p></Card>}<Card className="mt-4 border-amber-400/30 bg-amber-400/5 p-4"><p className="eyebrow">MONTHLY CHALLENGE</p><h2 className="mt-1 font-black">{data.challenge.title}</h2><p className="mt-1 text-sm text-muted-foreground">{data.challenge.description}</p><button disabled={Boolean(data.challenge.completedAt)} onClick={()=>void completeChallenge()} className="btn btn-secondary mt-3">{data.challenge.completedAt?'Completed':`Claim +${data.challenge.exp} EXP`}</button></Card><section className="mt-7"><div className="mb-3 flex items-end justify-between gap-3"><div className="min-w-0"><p className="eyebrow">SHOW UP FOR YOURSELF</p><h2 className="mt-1 text-xl font-black">Today’s habits</h2></div><Link className="flex shrink-0 items-center gap-1 text-sm font-bold text-brand" href="/library">Add habit <Plus className="size-4"/></Link></div>{data.tasks.length?<div className="task-grid grid gap-3">{data.tasks.map(task=><TaskCard key={task.id} task={task} onComplete={complete}/>)}</div>:<Card className="p-8 text-center"><div className="mx-auto grid size-14 place-items-center rounded-2xl bg-brand/10 text-2xl">🌱</div><h2 className="mt-3 font-black">Make today yours</h2><p className="mt-1 text-sm text-muted-foreground">Pick a habit from the library to begin.</p><Link className="btn btn-primary mt-4" href="/library">Explore habits <ArrowRight className="size-4"/></Link></Card>}</section>{toast&&<div role="status" className="fixed bottom-24 left-1/2 z-20 max-w-[calc(100vw-2rem)] -translate-x-1/2 rounded-full bg-foreground px-5 py-3 text-center text-sm font-bold text-background shadow-xl">{toast}</div>}<Nav deemphasizeDiscovery={!data.discoverUnlocked}/></main>
}
