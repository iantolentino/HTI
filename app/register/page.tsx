'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function Register(){const [error,setError]=useState('');const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const form=new FormData(e.currentTarget);const r=await fetch('/api/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password'),displayName:form.get('displayName'),timezone:Intl.DateTimeFormat().resolvedOptions().timeZone})});setBusy(false);if(!r.ok){setError((await r.json()).error);return}router.push('/onboarding')}return <main className="shell"><form onSubmit={submit} className="card mx-auto max-w-md p-6"><a href="/" className="font-black text-brand">⚡ LEVELUP DAILY</a><h1 className="mt-6 text-3xl font-black">Begin your run</h1><p className="mt-2 text-[rgb(var(--muted))]">Your first seven days are a grace period. Just show up.</p>{error&&<p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<label className="mt-5 block text-sm font-bold">Display name<input required name="displayName" minLength={2} className="mt-1 w-full rounded-xl border p-3" placeholder="Your name"/></label><label className="mt-4 block text-sm font-bold">Email<input required name="email" type="email" className="mt-1 w-full rounded-xl border p-3" placeholder="you@example.com"/></label><label className="mt-4 block text-sm font-bold">Password<input required name="password" type="password" minLength={8} className="mt-1 w-full rounded-xl border p-3" placeholder="At least 8 characters"/></label><button disabled={busy} className="btn btn-primary mt-6 w-full">{busy?'Creating…':'Create account'}</button><p className="mt-4 text-center text-sm">Already play? <a className="font-bold text-brand" href="/login">Log in</a></p></form></main>}
