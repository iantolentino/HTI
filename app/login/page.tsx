'use client';import {signIn} from 'next-auth/react';import {useRouter} from 'next/navigation';import {useState} from 'react';
export default function Login(){const [error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError('');const f=new FormData(e.currentTarget);const r=await signIn('credentials',{redirect:false,email:f.get('email'),password:f.get('password')});if(r?.error){setError('That email and password do not match.');return}router.push('/dashboard');router.refresh()}return <main className="shell"><form onSubmit={submit} className="card mx-auto max-w-md p-6"><a href="/" className="font-black text-brand">⚡ LEVELUP DAILY</a><h1 className="mt-6 text-3xl font-black">Welcome back</h1>{error&&<p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<label className="mt-5 block text-sm font-bold">Email<input required name="email" type="email" className="mt-1 w-full rounded-xl border p-3"/></label><label className="mt-4 block text-sm font-bold">Password<input required name="password" type="password" className="mt-1 w-full rounded-xl border p-3"/></label><button className="btn btn-primary mt-6 w-full">Log in</button><p className="mt-4 text-center text-sm">New here? <a className="font-bold text-brand" href="/register">Create an account</a></p></form></main>}
