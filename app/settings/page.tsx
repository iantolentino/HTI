'use client';import {useState} from 'react';import {Nav} from '@/components/Nav';
export default function Settings(){const [dark,setDark]=useState(false);const [leader,setLeader]=useState(false);const [publicProfile,setPublic]=useState(false);return <main className={dark?'dark':''}><div className="shell"><h1 className="text-3xl font-black">Profile & settings</h1><section className="card mt-5 p-5"><div className="flex items-center gap-4"><span className="grid h-14 w-14 place-items-center rounded-full bg-brand/15 text-2xl">⚡</span><div><h2 className="font-black">Your journey</h2><p className="text-sm text-[rgb(var(--muted))]">Level 1 Novice · 42 lifetime EXP</p></div></div></section><section className="card mt-4 divide-y p-1"><Setting label="Dark mode" detail="Keep your selected palette, change the canvas." value={dark} onChange={setDark}/><Setting label="Anonymous leaderboard" detail="Share only your nickname and streak." value={leader} onChange={setLeader}/><Setting label="Public profile" detail="Share aggregate stats and contribution graph only." value={publicProfile} onChange={setPublic}/></section>{publicProfile&&<p className="mt-3 rounded-xl bg-brand/10 p-3 text-sm">Your share link: levelup.daily/u/your-private-link</p>}<section className="card mt-4 p-5"><h2 className="font-black">Badges</h2><div className="mt-4 flex gap-4 text-center text-2xl"><span title="First steps">🌱</span><span title="7 day streak" className="opacity-30">🔥</span><span title="Perfect week" className="opacity-30">💫</span><span title="First prestige" className="opacity-30">👑</span></div></section><section className="card mt-4 p-5"><h2 className="font-black">Account</h2><label className="mt-3 block text-sm font-bold">Timezone<select className="mt-1 w-full rounded-xl border bg-surface p-3" defaultValue="Asia/Manila"><option>Asia/Manila</option><option>UTC</option><option>America/New_York</option></select></label><button className="btn btn-secondary mt-4">Change password</button><button className="ml-2 text-sm font-bold text-red-600">Delete account</button></section><Nav/></div></main>}
function Setting({label,detail,value,onChange}:{label:string;detail:string;value:boolean;onChange:(value:boolean)=>void}){return <label className="flex cursor-pointer items-center gap-3 p-4"><span className="flex-1"><b className="block">{label}</b><small className="text-[rgb(var(--muted))]">{detail}</small></span><input aria-label={label} checked={value} onChange={e=>onChange(e.target.checked)} type="checkbox" className="h-5 w-5 accent-brand"/></label>}
