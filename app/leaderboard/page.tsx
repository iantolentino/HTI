'use client';import {useEffect,useState} from 'react';import {Nav} from '@/components/Nav';
type Entry={nickname:string;currentStreak:number;longestStreak:number};export default function Leaderboard(){const [data,setData]=useState<{optedIn:boolean;entries:Entry[]}|null>(null);useEffect(()=>{fetch('/api/leaderboard').then(r=>r.ok?r.json():null).then(setData)},[]);return <main className="shell"><h1 className="text-3xl font-black">Streak leaders</h1><p className="mt-1 text-[rgb(var(--muted))]">Anonymous momentum, not comparison.</p><section className="card mt-5 p-5">{!data?<p className="animate-pulse">Finding the community…</p>:data.entries.length?data.entries.map((entry,index)=><div className="flex items-center gap-3 border-b-2 border-border py-3 last:border-0" key={`${entry.nickname}-${index}`}><b className="text-brand">#{index+1}</b><span className="text-xl">⚡</span><span className="flex-1 font-bold">{entry.nickname}<small className="mt-1 block text-xs text-muted-foreground">Best: {entry.longestStreak} days</small></span><b>{entry.currentStreak} days</b></div>):<div className="py-8 text-center"><div className="text-4xl">🌱</div><h2 className="mt-3 font-black">A quiet start</h2><p className="mt-1 text-sm text-[rgb(var(--muted))]">Be the first traveler to opt in.</p></div>}</section><p className="mt-5 rounded-xl bg-brand/10 p-4 text-sm">{data?.optedIn?'You are appearing anonymously.':'Want to join? Turn on anonymous leaderboard in Profile. Your habits and EXP remain private.'}</p><Nav/></main>}
